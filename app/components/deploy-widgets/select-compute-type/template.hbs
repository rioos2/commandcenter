<div class="layout1">
  {{svg/svg-create-cloud class="createcloud-ico"}}

  <p>{{t title}}</p>
</div>
<div class= {{if showInfo "display-none" "row m-0"}}>
  {{svg/svg-dots class="dots-ico"}}
  <p class="vert-description">
    {{t title}}
  </p>
  <p class="info" {{action "showDescription" on="click"}}>i</p>
  <div class="col-sm-6">
    {{svg/svg-cpu class=(if isSelectedCPU "cpu-ico selected" "cpu-ico")
        click=(action "cpuSelected")}}
  </div>
  <div class="col-sm-6">
    {{svg/svg-gpu class=(unless isSelectedCPU "gpu-ico selected" "gpu-ico")
        click=(action "gpuSelected")}}
  </div>
</div>
<div class= {{if showInfo "show-info row m-0" "display-none"}}>
  <p class="close-btn" {{action "closeDescription" on="click"}}>×</p>
  {{deploy-widgets/compute-type-description}}
</div>
