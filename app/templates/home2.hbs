<div class="stickybar">
    <a href="#maingauge"><label>Main Gauges</label></a>
    <a href="#statistics"><label>Statistics</label></a>
    <a href="#osusage"><label>OS usage by time</label></a>
</div>

<div class="home">
    {{!-- Main gauges --}}
    <div id="maingauge" class="main-gauges p-t-50 p-b-50">
        <p class="title p-l-50"><span class="underline">M</span>ain gauges</p>
        <div class="row">
            <div class="col-md-10 col-md-offset-1 col-lg-10 col-lg-offset-1">
               {{home2/dashboard-gauge model=model}}
            </div>
        </div>
    </div>

    <!-- {{!-- Statistics --}}
    <div id="statistics" class="statistics p-t-50 p-b-50">
        <p class="title p-l-50 p-b-20"><span class="underline">S</span>tatistics</p>
        <div class="row list-content">
            {{#each model.categorys as |category|}}
                <div class="col-md-6 col-sm-12 list">
                    {{home2/category-tab model=category}}
                </div>
            {{/each}}
        </div>
    </div>

    {{!-- OS useage by time    No horiz error. but not middle.         --}}
    <div id="osusage" class="os-useage-bytime p-t-50 p-b-50">
        <p class="title p-l-50 p-b-30"><span class="underline">O</span>s useage by time</p>
        {{home2/chart-os}}
    </div> -->
</div>
