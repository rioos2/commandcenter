<div class="contact-admin">
    <div class="container">
        <img class="logo-img" src="assets/images/logo2.png">
          <div class="col-container">
              <div class="page">
                <div class="modal-dialog modal-dialog-wizard-step">
                  <div class="modal-content">
                    <div class="modal-body">                      
                      {{#if model.invitation.id}}
                      <p>{{inviteMsg}}</p>
                      <a class="admin-hand" style="font-weight: bold;" {{action "accept"}}>{{t 'guardian.regular.organization.clickHere'}}</a>
                      &nbsp;{{inviteAcceptMsg}}
                      {{else}}
                      {{msg}}
                      {{/if}}
                    </div>
                  </div>
                </div>
              </div>
          </div>
    </div>
</div>
