<div class="dashboard">
	<div class="tabs">
		{{radio-button checked=(eq "infrastructure" selectedTab) value=selectedTab selection=selectedTab id="infrastructure" name="tab-control"}} {{radio-button checked=(eq "users" selectedTab) value=selectedTab selection=selectedTab id="users" name="tab-control"}} {{radio-button checked=(eq "settings" selectedTab) value=selectedTab selection=selectedTab id="settings" name="tab-control"}}
		<ul class="dashboardNav">
			{{dojos/basic-tab panels=panels selectedTab=selectedTab tab="infrastructure"}}
			{{dojos/basic-tab panels=panels selectedTab=selectedTab tab="users"}}
			{{dojos/basic-tab panels=panels selectedTab=selectedTab tab="settings" }}
		</ul>
		<div class="slider">
			<div class="indicator"></div>
		</div>
		<div class="content">
			{{#each panels as |tab|}}
				{{component tab selectedTab=selectedTab model=model reloadModel="reloadModel" modelSpinner=modelSpinner}}
			{{/each}}
		</div>
	</div>
</div>
